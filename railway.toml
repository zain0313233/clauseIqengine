[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Port binding is handled by Dockerfile CMD (shell expands $PORT correctly).
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
